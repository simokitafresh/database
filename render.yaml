# DEPRECATED: This file is no longer used.
# Render deployment now uses .env.render file instead.
# See .env.render for the current production configuration.

# Original render.yaml content (kept for reference):
# services:
#   - type: web
#     name: stockdata-api
#     env: python
#     plan: starter
#     autoDeploy: true
#     healthCheckPath: /healthz
#     buildCommand: pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt
#     startCommand: gunicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2 --worker-class uvicorn.workers.UvicornWorker --timeout 60
#     envVars: [environment variables moved to .env.render]
